This document describes programming paradigms required in the project.

Use OO paradigm.

Use clear separation between data-only specification objects (like `Chore`) and operational logic (like `RenewalManager`).